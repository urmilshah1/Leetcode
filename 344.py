class Solution:
    def reverseString(self, s: List[str]) -> None:
        s.reverse()
        
''' one of the more easier questions, there were a couple of ways i could think of doing this. The conditions were in place reverse string.
Hence simply using the reverse function built in python works just fine'''